import { describe,it,expect } from 'vitest';import { normalizeText,compactText,isDexNumber,parseDexNumber,classifySpecialForm } from '../src/utils/pokemonUtils';
describe('pokemonUtils',()=>{it('normaliza nombres humanos',()=>{expect(normalizeText("Farfetch'd")).toBe('farfetchd');expect(compactText('Zoroark Hisui')).toBe('zoroarkhisui');});it('detecta números de Pokédex',()=>{expect(isDexNumber('# 25')).toBe(true);expect(parseDexNumber('# 25')).toBe(25);});it('clasifica formas regionales',()=>{expect(classifySpecialForm('zoroark-hisui')).toBe('Forma de Hisui');expect(classifySpecialForm('raichu-alola')).toBe('Forma de Alola');expect(classifySpecialForm('charizard-mega-x')).toBe('Mega Evolución');});});
