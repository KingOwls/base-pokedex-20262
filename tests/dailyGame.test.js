import { describe,it,expect } from 'vitest';import { deterministicIndex,dayKey,buildDailyClues } from '../src/utils/dailyGame';
describe('daily game',()=>{it('es determinista por fecha',()=>{expect(deterministicIndex('2026-09-11',100)).toBe(deterministicIndex('2026-09-11',100));expect(dayKey(new Date(2026,8,11))).toBe('2026-09-11');});it('genera dirección de altura',()=>{const mk=(name,h)=>({pokemon:{name,height:h,weight:100,types:[{type:{name:'normal'}}]},species:{generation:{name:'generation-i'},is_legendary:false,is_mythical:false,varieties:[{is_default:true,pokemon:{name}}]}});expect(buildDailyClues(mk('a',5),mk('b',10)).height.direction).toBe('↑');});});
