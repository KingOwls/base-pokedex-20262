import TypeBadge from '../common/TypeBadge';
import { classifySpecialForm,padDex,spriteArtwork,titleCase } from '../../utils/pokemonUtils';
export default function PokemonHero({pokemon,species}){const def=species.varieties?.find(v=>v.is_default)?.pokemon?.name;const variant=def&&pokemon.name!==def;const spanish=species.names?.find(x=>x.language?.name==='es')?.name;const name=variant?titleCase(pokemon.name):(spanish||titleCase(species.name));return <article className="pokemon-hero"><div className="hero-grid"><div className="hero-art">{spriteArtwork(pokemon)?<img src={spriteArtwork(pokemon)} alt={name}/>:<div className="empty-state">Sin artwork</div>}</div><div className="hero-copy"><span className="pokedex-number">{padDex(species.id)}</span><h2>{name}</h2><div className="type-list">{pokemon.types.sort((a,b)=>a.slot-b.slot).map(x=><TypeBadge key={x.type.name} type={x.type.name}/>)}</div><div className="hero-tags">{species.is_legendary&&<span className="tag">⭐ Legendario</span>}{species.is_mythical&&<span className="tag">✨ Mítico</span>}{variant&&<span className="tag">🧬 {classifySpecialForm(pokemon.name)}</span>}</div></div></div></article>;}
