import { useState,useEffect } from 'react';
import { padDex,titleCase } from '../../utils/pokemonUtils';
const art=id=>`https://raw.githubusercontent.com/PokeAPI/sprites/master/sprites/pokemon/other/official-artwork/${id}.png`;
export default function ResultsGrid({items=[],title,onSelect}){const[visible,setVisible]=useState(40);useEffect(()=>setVisible(40),[items]);if(!items.length)return null;return <section className="results-section"><div className="section-heading"><div><p className="eyebrow">Resultados</p><h2>{title||'Pokémon encontrados'}</h2></div><span className="count-badge">{items.length}</span></div><div className="results-grid">{items.slice(0,visible).map(x=><button key={x.name} className="result-card" onClick={()=>onSelect(x.name)}><img src={art(x.id)} alt={titleCase(x.name)} loading="lazy"/><p>{x.isVariant||x.id>=10000?'Forma / variedad':padDex(x.id)}</p><h3>{titleCase(x.name)}</h3></button>)}</div>{visible<items.length&&<button className="secondary-button show-more" onClick={()=>setVisible(v=>v+40)}>Mostrar más</button>}</section>;}
