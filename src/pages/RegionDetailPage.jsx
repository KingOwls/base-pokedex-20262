import {REGIONS} from '../data/regions.js';import {Link} from '../routes/router.jsx';import {assetUrl} from '../utils/assets.js';
export default function RegionDetailPage({slug}){const r=REGIONS.find(x=>x.slug===slug);if(!r)return <div className="full-state">Región no encontrada.</div>;return <section className="region-detail" style={{'--accent':r.accent}}><div className="region-hero"><div><span className="kicker">ARCHIVO REGIONAL · GENERACIÓN {r.roman}</span><h1>{r.name}</h1><p>{r.blurb}</p><Link className="primary-link" to={`/pokedex/${r.slug}`}>Abrir Pokédex de {r.name}</Link></div><img src={assetUrl(`crowleth/${r.asset}`)} alt={`Pokédex ${r.name}`}/></div><div className="region-facts"><article><strong>{r.range[0]}–{r.range[1]}</strong><span>Rango generacional</span></article><article><strong>{r.special?'Histórica':'Principal'}</strong><span>Tipo de archivo</span></article><article><strong>PokéAPI</strong><span>Fuente de datos</span></article></div></section>}
