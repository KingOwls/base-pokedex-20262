import {GENERATIONS,NATIONAL} from '../data/generations.js';import {Link} from '../routes/router.jsx';import CrowlethNote from '../components/common/CrowlethNote.jsx';import {assetUrl} from '../utils/assets.js';
function Card({g}){return <Link to={`/pokedex/${g.slug}`} className="generation-card" style={{'--accent':g.accent}}><img src={assetUrl(`crowleth/${g.asset}`)} alt={`Pokédex ${g.name}`}/><div><span>Generación {g.roman}</span><h3>{g.name}</h3><p>{g.slug==='national'?'#001–#1025':`#${String(g.range[0]).padStart(3,'0')}–#${String(g.range[1]).padStart(3,'0')}`}</p></div></Link>}
export default function PokedexLibraryPage(){return <section className="page parchment"><div className="section-title"><span>ARCHIVOS DE INVESTIGACIÓN</span><h1>Biblioteca de Pokédex</h1><p>Selecciona una región o abre el archivo nacional de las nueve generaciones.</p></div><div className="generation-grid">{GENERATIONS.map(g=><Card key={g.slug} g={g}/>) }<Card g={NATIONAL}/></div><CrowlethNote>Los sprites pixel art identifican rápidamente cada especie; al abrir una ficha, el artwork y los datos de PokéAPI toman el protagonismo.</CrowlethNote></section>}
