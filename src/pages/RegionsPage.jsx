import {REGIONS} from '../data/regions.js';import {Link} from '../routes/router.jsx';import {assetUrl} from '../utils/assets.js';
export default function RegionsPage(){return <section className="page parchment"><div className="section-title"><span>ATLAS CROWLETH</span><h1>Regiones Pokémon</h1><p>Explora las nueve generaciones y el archivo histórico de Hisui. Esta sección es enciclopédica y no contiene RPG ni captura.</p></div><div className="regions-grid">{REGIONS.map(r=><Link className="region-card" key={r.slug} to={`/regions/${r.slug}`} style={{'--accent':r.accent}}><img src={assetUrl(`crowleth/${r.asset}`)} alt=""/><div><span>Generación {r.roman}</span><h2>{r.name}</h2><p>{r.blurb}</p><b>Explorar región →</b></div></Link>)}</div></section>}
