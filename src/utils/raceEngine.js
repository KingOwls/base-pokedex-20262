function rng(seed){let t=seed>>>0;return()=>{t+=0x6D2B79F5;let r=Math.imul(t^t>>>15,1|t);r^=r+Math.imul(r^r>>>7,61|r);return((r^r>>>14)>>>0)/4294967296;};}
export function seededRace(roster,seed=Date.now()){const random=rng(seed);const events=['Excelente salida','Mala salida','Tropiezo leve','Acelerón temporal','Fatiga','Segundo aire','Ritmo perfecto'];return roster.map(r=>{const knowledge=r.speed*.58+r.acceleration*.16+r.stamina*.14+r.consistency*.12;const uncertainty=(random()-.5)*18;const event=events[Math.floor(random()*events.length)];const modifier=event==='Excelente salida'?6:event==='Mala salida'?-6:event==='Tropiezo leve'?-5:event==='Acelerón temporal'?5:event==='Fatiga'?-4:event==='Segundo aire'?4:event==='Ritmo perfecto'?5:0;return{...r,score:knowledge+uncertainty+modifier,event};}).sort((a,b)=>b.score-a.score).map((r,i)=>({...r,place:i+1}));}
